// first C++ program

#include <iostream>   //Including header part (like import in python)  // #=preprocessor
using namespace std;   //standard namespace avoid naming collision

int main() {
	
	int x;   //float= decimal (4 bits)       double= decimal but for large value (8 bits)    int=integers (4 bits)
	x=12;
	cout << "Hello world"<<endl;
	cout<<"This is first c++ class\n";
	cout << "Value of x is:"<<x;
	return 0;
		
}
